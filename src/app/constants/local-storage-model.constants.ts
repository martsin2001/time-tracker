import { DatabaseModel } from '../models/database.models';

export const LOCAL_STORAGE_MODEL: DatabaseModel = {
  activeEntry: null,
  updateEntry: null,
  entriesList: [],
};

export const TIME_TRACKER_LOCAL_STORAGE = 'TIME_TRACKER_LOCAL_STORAGE';
